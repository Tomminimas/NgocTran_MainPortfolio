{
  "name": "ngoc-portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static HTML - no build needed'",
    "start": "echo 'Static HTML - use serve script for local development'",
    "serve": "python3 -m http.server 8000",
    "deploy": "gh-pages -d ."
  },
  "devDependencies": {
    "gh-pages": "^6.1.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}